# 🧰 项目工具脚本说明表

本项目包含多个辅助 `.bat` 脚本，用于加速开发、部署、同步操作，以下为各脚本的中文说明。

| 脚本文件名                     | 功能说明                             |
|-------------------------------|--------------------------------------|
| 📁 初始化项目目录结构.bat     | 创建 `tools/` 与 `local_scripts/` 并添加 `.gitkeep` 占位文件 |
| 🧹 清除上传错误的中文脚本.bat | 移除已上传至 GitHub 的中文命名脚本，确保 `.gitignore` 生效 |
| 📦 项目压缩备份.bat           | 自动压缩整个项目，打包成 zip 归档保存 |
| 📥 拉取 GitHub 更新.bat       | 一键从 GitHub 拉取最新变更 |
| 🚀 上传到 GitHub.bat          | 添加+提交+推送当前更改到 GitHub |
| 🚀 推送更新到 GitHub.bat      | 专用于推送已经 `add` 的内容，适合补充上传 |
| 🧠 启动控制台入口.bat         | 启动 AI 控制台或主入口模块（如 `main.py`）|
| 💻 打开 VSCode 开发.bat       | 自动打开当前目录到 VSCode |
| 🧪 install_requirements.bat   | 安装依赖环境（调用 `requirements.txt`）|

---

📌 **备注说明：**

- 所有 `.bat` 文件建议保留在根目录或 `tools/` 文件夹中管理
- 中文命名 `.bat` 文件默认不会上传（已被 `.gitignore` 忽略）
- 你可用 `generate_chinese_bat_names.py` 自动恢复中文命名脚本

